// this file is specifically used to export the proper files
const Square = require('./Square');
const Circle = require('./Circle');
const Triangle = require('./Triangle');
module.exports = { Circle, Triangle, Square };